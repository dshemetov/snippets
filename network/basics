# Allow application through firewall
sudo ufw allow ssh

# Network information (local address)
# LAN (Windows)
ipconfig
# LAN (Linux)
ifconfig
# LAN (Mac)
ifconfig en0 | grep inet

# Get my WAN address
host myip.opendns.com resolver1.opendns.com | grep "has address"
